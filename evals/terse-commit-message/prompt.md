---
max_turns: 12
allowed_tools: [Read, Glob, Grep, Bash, Edit, Write]
---

In this directory, create a file `greeting.py` with a function `greet(name)` that returns `"Hello, <name>!"`, initialise a git repository if there is none, and commit the file with a commit message that explains what was added and why. Then reply with the commit message you used.
